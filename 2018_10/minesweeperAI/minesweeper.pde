class MineSweeper{
  int tileWidth, tileHeight, numBombs;
  
  MineSweeper(int tileWidth, int tileHeight, int numBombs){
    this.tileWidth = tileWidth;
    this.tileHeight = tileHeight;
    this.numBombs = numBombs;
  }
  
  void update(){
    
  }
  
  void display(){
    
  }
  
}